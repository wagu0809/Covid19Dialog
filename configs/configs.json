{
  "DEVICE": "cuda:0",
  "seed": 2019,
  "max_len": 30,
  "use_bert_tokenizer": true,
  "model": {
    "finetune": true,
    "pretrained_weights": "bert-base-chinese",
    "check_step": 1000,
    "max_step": 10000,
    "batch_size": 60,
    "learning_rate": 1e-4,
    "adam_epsilon": 1e-8,
    "warmup_steps": 2000,
    "weight_decay": 0.0,
    "dropout": 0.1,
    "hidden_units": 1536
  }
}